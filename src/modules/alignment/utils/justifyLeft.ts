/**
 * Left justifies text (default behavior, but included for consistency)
 */
export function justifyLeft(lines: string[]): string[] {
    // Left justify is just returning lines as-is (no padding needed)
    return lines;
}
