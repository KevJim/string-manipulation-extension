# Phase 1: Project Initialization

## Steps

1. Generate project using yo code (TypeScript).

   - Do not install yo as global extension
   - The project should use TypeScript

2. Install dependencies:

   - `npm install change-case`.
   - The project should use prettier
   - The project should use eslint

3. Configure package.json categories and activation events.
